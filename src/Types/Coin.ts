
type ICoin = {
    baseID: String,
    cellID: String,
    coinID: String,
    color: String,
    isRetired: Boolean,
    isSpawned: Boolean,
    position: String,
    steps: Number,
}

export { ICoin }